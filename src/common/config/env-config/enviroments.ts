export const archivosEnv = {
  desarrollo: '.env',
  produccion: '.prod.env',
  testeo: '.test.env',
};

export enum Entornos {
  desarrollo = 'desarrollo',
  produccion = 'produccion',
  test = 'test',
}
